import React from 'react'
import { Panel } from '@vkontakte/vkui'

const Friends = ({ id, title }) => {
  return (
    <Panel id={id}>
      <h1>Friends</h1>
    </Panel>
  )
}

export default Friends
